import React from "react";

const FS1 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "10px" }}>
      {props.children}
    </span>
  );
};

const FS2 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "12px" }}>
      {props.children}
    </span>
  );
};

const FS3 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "14px" }}>
      {props.children}
    </span>
  );
};

const FS4 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "16px" }}>
      {props.children}
    </span>
  );
};

const FS5 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "18px" }}>
      {props.children}
    </span>
  );
};

const FS6 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "20px" }}>
      {props.children}
    </span>
  );
};
const FS7 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "22px" }}>
      {props.children}
    </span>
  );
};

const FS8 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "24px" }}>
      {props.children}
    </span>
  );
};

const FS9 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "26px" }}>
      {props.children}
    </span>
  );
};
const FS10 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "28px" }}>
      {props.children}
    </span>
  );
};

const FS11 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "30px" }}>
      {props.children}
    </span>
  );
};

const FS12 = (props) => {
  return (
    <span {...props.attr} style={{ fontSize: "32px" }}>
      {props.children}
    </span>
  );
};
export { FS1, FS2, FS3, FS4, FS5, FS6, FS7, FS8, FS9, FS10, FS11, FS12 };
